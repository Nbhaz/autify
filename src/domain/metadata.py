class Metadata:
    def __init__(self, filename, links, images, last_fetch):
        self.filename = filename
        self.links = links
        self.images = images
        self.lastFetch = last_fetch
